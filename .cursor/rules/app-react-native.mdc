---
description: React Native 移动端开发约束
globs: app/**/*
alwaysApply: false
---

# App (React Native)

- **React Native 0.76.9** / **React 18.3**（锁定 `18.3.1`）/ **TypeScript 5.0.4**
- **Node ≥18**（推荐 20 LTS，见根目录 `.nvmrc`）
- 禁止 RN < 0.76、React 非 18.3
- 目标平台: Android + iOS
- 屏幕放 `src/screens/`，导航放 `src/navigation/`
- API 请求集中在 `src/api/`
- 不直连数据库，只通过 backend API
- 后续用 EAS / 原生构建打包
- 版本详情见 `.cursor/rules/versions.mdc`
